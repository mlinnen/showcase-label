<Window x:Class="ShowcaseLabel.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About Showcase Label Printer"
        Width="360"
        Height="180"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Showcase Label Printer" FontSize="18" FontWeight="Bold"/>
        <TextBlock Grid.Row="1" Text="{Binding VersionText}" Margin="0,8,0,0"/>
        <Button Grid.Row="3" Content="Close" IsDefault="True" IsCancel="True" Click="CloseButton_Click"
                Width="80" HorizontalAlignment="Right" Padding="8,4"/>
    </Grid>
</Window>
